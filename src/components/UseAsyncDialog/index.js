import ReUseAsyncDialog from './src/UseAsyncDialog.vue';

/* istanbul ignore next */
ReUseAsyncDialog.install = (Vue) => {
  Vue.component(ReUseAsyncDialog.name, ReUseAsyncDialog);
};

export default ReUseAsyncDialog;
