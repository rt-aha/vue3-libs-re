import ReReDateRangePicker from './src/ReDateRangePicker.vue';

/* istanbul ignore next */
ReReDateRangePicker.install = (Vue) => {
  Vue.component(ReReDateRangePicker.name, ReReDateRangePicker);
};

export default ReReDateRangePicker;
