import ReReInputTimer from './src/ReInputTimer.vue';

/* istanbul ignore next */
ReReInputTimer.install = (Vue) => {
  Vue.component(ReReInputTimer.name, ReReInputTimer);
};

export default ReReInputTimer;
