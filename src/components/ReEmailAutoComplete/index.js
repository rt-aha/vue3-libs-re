import ReReEmailAutoComplete from './src/ReEmailAutoComplete.vue';

/* istanbul ignore next */
ReReEmailAutoComplete.install = (Vue) => {
  Vue.component(ReReEmailAutoComplete.name, ReReEmailAutoComplete);
};

export default ReReEmailAutoComplete;
