import ReReDateTimePicker from './src/ReDateTimePicker.vue';

/* istanbul ignore next */
ReReDateTimePicker.install = (Vue) => {
  Vue.component(ReReDateTimePicker.name, ReReDateTimePicker);
};

export default ReReDateTimePicker;
